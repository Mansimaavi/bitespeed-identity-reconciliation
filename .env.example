# Database (required)
DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public"

# Server
PORT=3000
NODE_ENV=development

# Render: Set DATABASE_URL in Dashboard (from PostgreSQL instance).
# Render sets PORT automatically. NODE_ENV=production recommended.
